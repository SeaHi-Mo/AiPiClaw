RESOLVED|16a06bb|generate_204 routing works (abfed1d) + 204 status string + Content-Type + STA guard + p->tot_len + resp buffer
YELLOW|axk_config_portal.c:~320|Blind vTaskDelay(50ms) after TLS listener close — no task synchronization guarantee. s_tls_task may still be running. Use xTaskNotifyGive+ulTaskNotifyTake or vTaskDelete with task handle check.
YELLOW|axk_config_portal.c:329|On 500ms timeout expiration, stop() deletes s_tls_listener while acceptor task may still hold reference (s_tls_task non-NULL) — potential use-after-free on timeout path|Add s_tls_task=NULL check before delete, or use mutex to prevent concurrent access
YELLOW|axk_config_portal.c:658,666|snprintf return used as write length without truncation check; header[512] may overflow with long content_type; add header_len = (header_len >= (int)sizeof(header)) ? (int)sizeof(header)-1 : header_len
RESOLVED|ddd4d4c|302 responses lacked Location header — now use PORTAL_REDIRECT_302 macro with proper Location: http://192.168.4.1/ at all 3 call sites (generate_204, /redirect, catch-all)
YELLOW|axk_config_portal.c:645|send_response() case 302 is now dead code — no caller passes 302 after ddd4d4c migrated all 302 paths to direct netconn_write+PORTAL_REDIRECT_302. Remove or guard with #if 0.
YELLOW|axk_config_portal.c:644|send_response() case 204 is also dead code — no caller passes 204 to send_response. (The generate_204 endpoint now returns 302, not 204.)
BLUE|axk_config_portal.c:77|PORTAL_REDIRECT_302 lacks Access-Control-Allow-Origin: * header that all send_response() paths include. Low risk for captive portal probes (no CORS preflight), but inconsistency to note.
BLUE|axk_config_portal.c:1122,1138,1149|strlen(PORTAL_REDIRECT_302) computed at runtime; sizeof(PORTAL_REDIRECT_302)-1 is compile-time constant. Micro-optimization, not a bug.
BLUE|axk_config_portal.c:1098|Top-level doc comment says "Android: GET /generate_204 → expects 204 No Content" — technically accurate (Android's expectation), but now the code returns 302. In-code comment at L1115-1119 explains rationale, but the top-level block could be slightly confusing.
NOTE|axk_config_portal.c:1115-1123|Google /generate_204 path correctly returns PORTAL_REDIRECT_302 (proper Location header) — Android detects captive portal via unexpected 302 response, WebView follows Location to config page. No Android compatibility regression.
NOTE|axk_config_portal.c:1137-1139|/redirect probe path correctly uses PORTAL_REDIRECT_302. 
NOTE|axk_config_portal.c:1148-1150|Catch-all correctly uses PORTAL_REDIRECT_302 — all unmatched paths get proper 302 with Location header.
NOTE|Compilation|netconn_write already used in send_response() (L679, L685) via lwip/api.h; build should pass without issues.
YELLOW|axk_config_portal.c:443|portal_drain_backlog modifies s_listener; safe only if trigger_scan_and_collect always runs in portal_task context — verify call chain
YELLOW|axk_config_portal.c:714|NETCONN_NOCOPY for static HTML assumes CONFIG_UI_HTML/WEB_UI_HTML remain static const — add static_assert or comment to enforce invariant
BLUE|lwipopts_user.h:70|comment mixes Chinese/English — use consistent English comments per coding standards
BLUE|axk_config_portal.c:89|volatile on s_active_connections unnecessary for single-task access (harmless but misleading)
