YELLOW|axk_wifi_onboard.c: save_creds write uses ef_set_env (string API) but axk_wifi_auto_connect reads with ef_get_env_blob (blob API) → length mismatch, saved creds not restorable
YELLOW|axk_hal_wifi.c: axk_hal_wifi_connect() stack-local params passed to wifi_mgmr_sta_connect → same UAF as axk_wifi_connect (dead code, no callers)
YELLOW|lwipopts_user.h: MEMP_NUM_NETCONN 128→16, FD_SETSIZE removed → may cause accept ERR_MEM under browser multi-tab load
YELLOW|axk_wifi_manager.c:5801|axk_wifi_event_handler CODE_WIFI_ON_CONNECTING calls axk_wifi_set_state() without trylock from async callback context — risk queue.c assert if set_state takes mutex
YELLOW|axk_config_portal.c:1747|parse_request char req_buf[4096] + handle_client body[4096] ≈ 8KB on portal_task's 8KB stack — marginal, risk overflow with other locals/watchdog
YELLOW|axk_config_portal.c:2120|handle_wifi_connect uses ret==0?200:500 but ret always 0 at that point — dead 500 branch misleading
YELLOW|AiPiClaw/src/platform/axk_shell_dma.c:38|Ring_Buffer_Write called from DMA ISR callback chain — verify ISR-safety; concurrent DMA+RTO ISR may corrupt ring buffer if Ring_Buffer_Write is not reentrant|Guard with portENTER_CRITICAL or use ISR-safe Ring_Buffer_Write_FromISR
YELLOW|AiPiClaw/src/wifi/axk_wifi_manager.c:243|Infinite reconnect with fixed 5s delay and no retry cap; removed exponential backoff + max_retry logic — device loops forever on persistent disconnect|Add retry cap or escalating backoff
YELLOW|AiPiClaw/src/platform/axk_shell_dma.c:33|s_dma_ready (bool) not volatile; set pre-scheduler in axk_shell_dma_init(), read post-scheduler in axk_shell_dma_isr_attach() — compiler may optimize away the read|Add volatile qualifier
YELLOW|AiPiClaw/main.c:141|vTaskDelay(500) blind delay before axk_shell_dma_isr_attach(); if shell_task init takes >500ms, ISR replace races with shell's char ISR|Use binary semaphore signaled by shell_task after ISR attach, waited in mimiclaw task
YELLOW|AiPiClaw/src/onboard/axk_wifi_onboard.c:178|axk_wifi_onboard_stop() no longer calls wifi_mgmr_ap_stop(); SoftAP beaconing continues until reset|Restore wifi_mgmr_ap_stop() call
YELLOW|AiPiClaw/lwipopts_user.h:70|MEMP_NUM_NETCONN further reduced from 128 to 16; FD_SETSIZE removed — under concurrent WebSocket + HTTP + portal tabs, netconn_new() returns ERR_MEM|Keep at 32 minimum for WebSocket + HTTP concurrent safety
YELLOW|axk_config_portal.c:1590|dns_hijack_recv: calls axk_wifi_get_state() from lwIP RAW callback (tcpip_thread), takes FreeRTOS mutex with 100ms timeout; may block lwIP core. Fix: use volatile STA state flag, or check before binding PCB
YELLOW|axk_wifi_manager.c:140-169|event handler trylock(0) may silently drop retry_count/max_retry_exceeded updates when poll() holds mutex; reconnect tracking can miss disconnect events. Fix: use task notification queue or retry flag approach
YELLOW|axk_wifi_manager.c:240|axk_wifi_manager_poll() double mutex take pattern: take+release for event flags, then take again for reconnect — TOCTOU window between releases on g_wifi_ctx state
YELLOW|axk_wifi_manager.c:133|rapid-fire WiFi events (CONNECTED→DISCONNECT) overwrite s_evt_new_state before poll() processes — intermediate state lost
YELLOW|axk_wifi_manager.c:253|removed max retry limit and exponential backoff — continuous disconnect→reconnect infinite loop at fixed 5s delay
YELLOW|axk_config_portal.c:handle_apply|wifi_mgmr_ap_stop() return value not checked — silent failure leaves radio in AP mode, STA connect will fail on active AP radio
YELLOW|axk_config_portal.c:parse_request|atoi() for Content-Length wraps on crafted value >INT_MAX, triggers timeout-bounded read loop (DoS, bounded)
YELLOW|axk_config_portal.c:dns_hijack_recv|memcpy from p->payload for DNS qname copy — chained pbufs may fragment; use pbuf_copy_partial() for safety
YELLOW|axk_agent_loop.c:build_system_prompt|fallback prompt text uses literal \\n (doubled escape) instead of actual newlines; LLM system prompt corrupted with raw \\n characters everywhere|Replace \\\\n with \\n in fallback string literals
YELLOW|axk_wifi_manager.c:120|event_handler early return on mutex==NULL silently drops WiFi events during early boot before mutex init; events never recovered|Defer events or init mutex before wifi_mgmr_start
YELLOW|axk_shell_dma.c:105|hardcoded UART RDR register offset 0x8C (uart_shell->reg_base + 0x8C); magic number breaks portability across SDK versions|Use SDK-defined macro (e.g. UART_RDR_OFFSET)
YELLOW|axk_shell_dma.c:50-61|RTO ISR only clears UART_INTCLR_RTO; spurious non-RTO interrupts cause infinite ISR loop (interrupt storm)|Clear all pending UART interrupts defensively or mask to RTO-only at init
YELLOW|axk_wifi_onboard.c:148|SoftAP static IP (192.168.4.1) and use_ipcfg removed; DHCP-only means no IP if SoftAP DHCPD fails|Restore static IP as fallback or verify DHCPD always works
YELLOW|axk_wifi_manager.c:329-346|axk_wifi_connect holds g_wifi_ctx.mutex during wifi_mgmr_sta_connect() call; extended critical section may starve async callbacks trying trylock|Move xSemaphoreGive before wifi_mgmr_sta_connect as before, or reduce critical section scope
YELLOW|axk_wifi_manager.c:423-428|axk_wifi_disconnect same pattern: mutex held during wifi_sta_disconnect() — extends critical section|Release mutex before disconnect call
