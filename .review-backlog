YELLOW|AiPiClaw/src/gateway/axk_config_portal.c:133|netif_find(NULL) called without TCPIP core lock — other callers use LOCK_TCPIP_CORE(). Diagnostic-only so low risk, but inconsistent with codebase pattern.|Wrap in LOCK_TCPIP_CORE()/UNLOCK_TCPIP_CORE() per net_al.c:359-361 pattern.
YELLOW|AiPiClaw/main.c:163|Second axk_wifi_onboard_start() retry return value unchecked — if both attempts fail, code silently proceeds to config_portal_start which will fail to bind.|Check ret after second attempt; if still non-zero, skip config_portal_start and set longer delay before retry cycle.
BLUE|AiPiClaw/main.c:164|Magic number 500ms delay — hardcoded literal instead of named constant.|Define PORTAL_BIND_DELAY_MS 500 and use it.
YELLOW|axk_wifi_manager.c:179|TOCTOU: g_wifi_ctx.ssid read for snprintf after xSemaphoreGive at L172, may race with concurrent SSID updates|Capture ssid into local var while mutex held before formatting
YELLOW|axk_wifi_manager.c:164|Silent failure: xSemaphoreTake timeout on disconnect drops all retry state update, reconnect may never trigger|Log warning when mutex acquisition fails; consider deferred retry via timer
YELLOW|config_ui.h:238|_stepTimer global mutable — concurrent saveWiFi/saveLLM could race on timer reference|Use per-step local timers or guard with state flag
YELLOW|config_ui.h:290|scanWifi polling: renderAPList called outside promise chain, unhandled if throws|Wrap in try/catch or keep in chain
YELLOW|web_ui.h:367|WebSocket heartbeat (ping/pong) removed — silent TCP half-open disconnection won't be detected; no reconnect trigger|Restore 10s ping / 8s pong timeout or use WebSocket built-in ping
YELLOW|web_ui.h:501|el['scroll-btn'] not in DOM cache array (line 316-317) — newChat() silently fails to hide scroll button|Add 'scroll-btn' to el cache array
YELLOW|web_ui.h:271/744|Double click binding on scroll-btn — inline onclick + addEventListener both fire scrollToBottom()|Remove either inline onclick or addEventListener
YELLOW|web_ui.h:367|ws.onerror handler is empty — if onclose doesn't fire after error, no reconnect triggers|Add scheduleReconnect() call in onerror
YELLOW|axk_config_portal.c:385|wifi_mgmr_ap_start failure logged but unrecovered — portal permanently unreachable, no retry/reboot|add retry loop or trigger system reset on AP restart failure
YELLOW|axk_config_portal.c:391|bflb_device_get_by_name("watchdog0") called every scan poll iteration — wasteful device lookup in tight loop|hoist device pointer outside the while loop, lookup once before loop
YELLOW|axk_config_portal.c:428|portal_scan_and_cache() sets s_scan_cached=true even when trigger_scan_and_collect() failed (empty cache from failed boot pre-scan)|only set s_scan_cached=true when ret==0 and json_str non-NULL and non-empty
YELLOW|axk_config_portal.c:384|wifi_mgmr_ap_stop() return value unchecked; old code checked and returned -1 on failure — silent failure may cause subsequent scan to fail unpredictably
YELLOW|axk_config_portal.c:394-397|watchdog feed (bflb_wdg_reset_countervalue) removed from 5s Phase2 scan wait loop — potential watchdog reset on long scans
BLUE|axk_config_portal.c:770|axk_wifi_onboard_start() called synchronously in HTTP handler — blocks response until AP restarts (~500ms+); comment says "Avoid blocking" but call IS blocking
